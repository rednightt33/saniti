# Analytical environment available to model-generated code. Pinned; every analysis records the
# versions actually loaded. No package can be installed at runtime (the analysis process cannot
# execute programs or open sockets, and pip is removed from the image).
numpy==2.4.6
pandas==3.0.6
polars==1.44.2
pyarrow==25.0.1
duckdb==1.5.5
scipy==1.17.1
statsmodels==0.15.0
# TA-Lib 0.8.1 ships manylinux wheels that bundle the TA-Lib C library 0.8.1.
TA-Lib==0.8.1
matplotlib==3.11.2
